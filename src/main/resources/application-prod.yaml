server:
  address: 0.0.0.0
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/CoreFlow_prod
    username: postgres
    password: root@IET25
    driver-class-name: org.postgresql.Driver


upload:
  path: /uploads

app:
  firebase:
    service-account-path: ${FIREBASE_SERVICE_ACCOUNT_PATH:/opt/coreflow/secrets/firebase-service-account-adminsdk-coreflow.json}
  ocr:
    tesseract:
      cli: ${TESSERACT_CLI:tesseract}
